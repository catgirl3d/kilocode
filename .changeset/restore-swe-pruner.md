---
"@kilocode/cli": minor
"@kilocode/sdk": minor
"@kilocode/kilo-ui": minor
"kilo-code": minor
---

Restore opt-in task-aware pruning for large `read`, `grep`, and `bash` outputs with explicit model selection, pruning indicators, and bounded fail-open requests.
