---
"@kilocode/cli": minor
"kilo-code": minor
---

Add an optional on-demand `consult_advisor` tool. When `experimental.advisor_model` is configured, agents can request a second opinion from that model at planning checkpoints, when stuck, or before finishing non-trivial work.
The model can also be selected in the extension settings (Models tab).
